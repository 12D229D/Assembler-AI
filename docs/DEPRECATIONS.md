# Funciones Obsoletas (DEPRECATIONS.md)

## ⚠️ Obsolescencia

- **Backend PHP Directo**: Aunque la ruta `/api_handler.php` se mantiene activa para compatibilidad, la implementación subyacente en PHP ha sido reemplazada por Express.js en `server.ts`.
