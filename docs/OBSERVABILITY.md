# Observabilidad (OBSERVABILITY.md)

## 👁️ Métricas y Telemetría

- **Latencia de Solicitudes**: Tiempo medido entre el `POST /api/chat` y la devolución del payload por parte del SDK del LLM.
- **Tasa de Errores**: Monitoreo de códigos 502 (fallos en la llamada externa) y 422 (mensajes inválidos).
