# Inicio Rápido (QUICKSTART.md)

Ponte en marcha con **Assembler-AI** en menos de 2 minutos:

1. Ejecuta el servidor dev:
   ```bash
   npm run dev
   ```
2. Abre la URL en el navegador:
   `http://localhost:3000`

3. Haz tu primera pregunta sobre lenguaje ensamblador x86:
   > *"¿Cómo se implementa un bucle sencillo con el registro ECX en NASM 32-bit?"*
